<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-2.5.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd
       http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
       http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd
       http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${driverClassName}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
    </bean>

    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.jdbc.use_get_generated_keys">true</prop>
            </props>
        </property>
        <property name="packagesToScan" value="ru.ifmo.quant"/>
    </bean>

    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory"/>
    </bean>

    <bean id="dataService" class="ru.ifmo.quant.dao.DataServiceImpl"/>

    <bean id="commandFactory" class="ru.ifmo.quant.commands.CommandFactory">
        <property name="commands">
            <util:properties id="myProps" location="classpath:commands.properties"/>
        </property>
    </bean>

    <bean id="processContainer" class="ru.ifmo.quant.ProcessContainer"/>

    <bean id="messageHandler" class="ru.ifmo.quant.handlers.MessageHandler">
        <property name="processContainer" ref="processContainer"/>
        <property name="dataService" ref="dataService"/>
        <property name="commandFactory" ref="commandFactory"/>
        <property name="sendingPool" ref="SendingPool"/>
    </bean>

    <bean id="telegramHandler" class="ru.ifmo.quant.handlers.telegram.TelegramHandler">
        <property name="botName" value="${telegramBotUsername}"/>
        <property name="botToken" value="${telegramBotToken}"/>
        <property name="telegramBotsApi">
            <bean class="org.telegram.telegrambots.TelegramBotsApi"/>
        </property>
        <property name="messageHandler" ref="messageHandler"/>
    </bean>

    <bean id="SendingPool" class="ru.ifmo.quant.executors.MessagesPool">
        <property name="telegramHandler" ref="telegramHandler"/>
        <property name="dataService" ref="dataService"/>
    </bean>

    <!-- Activates various annotations to be detected in bean classes -->
    <context:annotation-config/>

    <!-- @Transaction annotation support -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- Need for Repository abstraction -->
    <jpa:repositories base-package="ru.ifmo.quant.dao" entity-manager-factory-ref="entityManagerFactory"
                      transaction-manager-ref="transactionManager"/>

    <!-- Need for TaskExecutor and TaskSheduler abstractions -->
    <task:annotation-driven executor="executor" scheduler="scheduler"/>
    <task:executor id="executor" pool-size="5"/>
    <task:scheduler id="scheduler" pool-size="10"/>

    <!-- Scanning components in base-package (look for annotations) -->
    <context:component-scan base-package="ru.ifmo.quant"/>

    <context:property-placeholder location="*.properties"/>
</beans>